<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Vistora – Discover & Share Visual Ideas</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .fade-in-up {
      animation: fadeInUp 0.7s ease-out both;
    }

    .gradient-text {
      background: linear-gradient(to right, #ec4899, #8b5cf6);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .btn-glow {
      background: linear-gradient(to right, #ec4899, #8b5cf6);
      color: white;
      transition: all 0.3s ease;
      box-shadow: 0 0 0 transparent;
    }

    .btn-glow:hover {
      box-shadow: 0 0 12px rgba(236, 72, 153, 0.7);
      transform: scale(1.05);
    }

    .hero-bg {
      background: linear-gradient(to right, #1f2937, #111827);
    }
  </style>
</head>
<body class="bg-zinc-900 text-white font-sans leading-relaxed">

  <!-- Navbar -->
<!-- Navbar -->
<nav class="w-full py-6 px-10 flex justify-between items-center shadow-md border-b border-zinc-700 hero-bg z-50">
  <a href="/" class="text-3xl font-bold gradient-text tracking-tight">Vistora</a>

  <!-- Conditional Links -->
  <div class="space-x-6 hidden md:flex text-sm font-medium">
    <% if (user) { %>
      <a href="/profile" class="hover:text-pink-400 transition">Profile</a>
      <a href="/feed" class="hover:text-pink-400 transition">Feed</a>
      <a href="/logout" class="hover:text-pink-400 transition">Logout</a>
    <% } else { %>
      <a href="/login" class="hover:text-pink-400 transition">Login</a>
      <a href="/register" class="hover:text-pink-400 transition">Sign Up</a>
    <% } %>
  </div>
</nav>


  <!-- Hero Section -->
  <section class="hero-bg pt-24 pb-32 text-center">
    <div class="max-w-3xl mx-auto px-6 fade-in-up">
      <h1 class="text-5xl md:text-6xl font-bold tracking-tight leading-tight mb-6">
        Discover. Save. Inspire. <br/>
        <span class="gradient-text">Welcome to Vistora.</span>
      </h1>
      <p class="text-zinc-400 text-lg md:text-xl mb-8">
        Your visual world, your way — explore ideas, create collections, and share your inspiration.
      </p>
      <a href="/register" class="inline-block px-8 py-3 rounded-full text-lg font-semibold btn-glow">
        Get Started
      </a>
    </div>
  </section>

  <!-- Features Section -->
  <section class="bg-zinc-800 py-20 px-6">
    <div class="max-w-6xl mx-auto grid md:grid-cols-3 gap-12 text-center fade-in-up">
      <div>
        <i class="ri-image-add-line text-4xl text-pink-500 mb-4"></i>
        <h3 class="text-xl font-semibold mb-2">Upload Beautiful Posts</h3>
        <p class="text-zinc-400">Showcase your ideas with stunning images that catch attention.</p>
      </div>
      <div>
        <i class="ri-pushpin-line text-4xl text-pink-500 mb-4"></i>
        <h3 class="text-xl font-semibold mb-2">Create Inspiring Pins</h3>
        <p class="text-zinc-400">Organize your visual ideas into pins and boards effortlessly.</p>
      </div>
      <div>
        <i class="ri-heart-line text-4xl text-pink-500 mb-4"></i>
        <h3 class="text-xl font-semibold mb-2">Like, Comment & Connect</h3>
        <p class="text-zinc-400">Engage with creators and grow your visual network.</p>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-br from-pink-600 to-purple-700 text-center">
    <h2 class="text-3xl md:text-4xl font-bold mb-6">Join Vistora today</h2>
    <p class="mb-8 text-white/90 text-lg max-w-xl mx-auto">Start saving, organizing, and discovering your favorite visual inspirations.</p>
    <a href="/register" class="inline-block px-8 py-3 rounded-full bg-white text-pink-700 font-semibold text-lg shadow-lg hover:scale-105 transition">Sign Up Free</a>
  </section>

  <!-- Footer -->
  <footer class="bg-zinc-900 border-t border-zinc-700 py-6 text-center text-sm text-zinc-500">
    © <%= new Date().getFullYear() %> Vistora. Built with Ramdas Hembram💖.
  </footer>

</body>
</html>
